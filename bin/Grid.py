import numpy as np

class Grid():
    
    def __init__(self, nx, ny, params):
        self.nx = nx
        self.ny = ny
        self.w  = np.zeros((nx, ny))
        self.dw = np.zeros((nx, ny))
        self.params = params
    
    def map_results(inputGrid):
        '''
        No clue what this is supposed to do

        '''
        return 0
    
    def solve():
        '''
        Again no idea what this should be doing
        '''
        return 0